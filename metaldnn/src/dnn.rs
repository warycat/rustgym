// use crate::layer::*;

#[derive(Debug)]
pub struct Dnn {
    // #[derivative(Debug = "ignore")]
// device: StrongPtr,
// #[derivative(Debug = "ignore")]
// layers: Vec<Box<dyn Layer>>,
}

impl Dnn {
    pub fn new() -> Self {
        // let layers = vec![];
        Dnn {
                // layers,
            }
    }

    // pub fn sequential_model(&mut self, layers: Vec<Box<dyn Layer>>) {
    //     for mut layer in layers {
    //         // layer.init_with_device(self.device);
    //         self.layers.push(layer);
    //     }
    // }
}
